# DEPLOYMENT_READINESS_REPORT

**نطاق الجرد:** قراءة فقط. لم يُشغَّل بناء أو اختبار جديد، ولم يُنفَّذ نشر أو رفع إلى GitHub أو Cloudflare، ولم يُعدَّل أو يُحذف أي ملف قائم. الملف الوحيد المنشأ في هذه المراجعة هو هذا التقرير.

## الخلاصة التنفيذية

التطبيق **جاهز تقنيًا للنقل إلى Cloudflare Pages كتطبيق ثابت (Static PWA)**، وجاهز للإطلاق التجريبي المحدود بعد إتمام قائمة ما قبل الإطلاق أدناه. رحلة المعلم، مساحة المدير، التخزين المحلي، PWA، وتصدير PDF وWord وExcel تعمل في المتصفح ولا تحتاج إلى خادم أو قاعدة بيانات لتشغيل الوظائف الحالية. الاستثناء المهم ليس تقنيًا في الاستضافة بل تشغيليًا: بيانات IndexedDB مرتبطة بأصل الموقع، لذلك لن تنتقل تلقائيًا عند تغيير النطاق؛ يجب حفظ نسخ احتياطية وملفات صريحة قبل الانتقال. [1]

| محور التقييم | الحالة | الحكم |
|---|---:|---|
| تشغيل الواجهة الأساسية | جاهز | React/Vite، واجهة عربية RTL، وتخزين محلي.
| العمل دون اتصال | جاهز | Manifest وService Worker وWorkbox مكوّنة ضمن Vite PWA.
| تصدير PDF/Word/Excel | جاهز | التوليد محلي داخل المتصفح، دون خدمة توليد خارجية.
| تطبيق المدير | جاهز محليًا | يعمل على `/director` ويحفظ PDF والتقييمات في IndexedDB.
| الاستضافة الثابتة | جاهز بشروط | Cloudflare Pages مناسب؛ يلزم اختبار فتح `/director` مباشرة بعد أول معاينة منشورة.
| الإطلاق العام واسع النطاق | يحتاج بوابة تشغيلية | النسخ الاحتياطي قبل تغيير النطاق، سياسة خصوصية، تجربة أجهزة حقيقية، ودعم مستخدمين.

> **قرار الإطلاق:** ابدأ بإطلاق تجريبي محدود (Pilot) بعد اختبار المجال الإنتاجي والنسخ الاحتياطية. لا يوجد مانع معماري من الاستضافة الثابتة، لكن لا تعتبر تغيير النطاق ترحيلًا للبيانات المحلية.

## جرد البنية والإخراج

| البند | النتيجة الدقيقة |
|---|---|
| نوع التطبيق | React 19 + Vite 7 + TypeScript + Tailwind، تطبيق صفحة واحدة مع مسار `/` للمعلم و`/director` للمدير. |
| نوع الاستضافة المناسب | Static hosting / PWA. لا تتطلب الوظائف الحالية Node أو Express في وقت التشغيل. |
| أمر التطوير | `pnpm dev` |
| أمر البناء الحالي | `pnpm build` |
| ما ينفذه البناء الحالي | `vite build` ثم تجميع `server/index.ts` باستخدام esbuild. |
| مجلد ملفات الويب القابل للنشر | `dist/public`، وفق إعداد `vite.config.ts`. |
| ملف خادم إضافي | ينشئ البناء `dist/index.js` لخادم Express ثابت. لا يُرفع إلى Cloudflare Pages ولا يلزم للتشغيل هناك. |
| أمر Cloudflare Pages المقترح الآن | Build command: `pnpm build`؛ Build output directory: `dist/public`؛ Root directory: جذر المستودع. Cloudflare Pages يسمح بتحديد أمر البناء ومجلد الإخراج صراحةً. [2] |
| مدير الحزم | `pnpm`، مع قفل إصدارات في `pnpm-lock.yaml` وتعريف `packageManager` في `package.json`. |

## هل التطبيق Static بالكامل؟

نعم، **السلوك الفعلي للتطبيق Static بالكامل**. طبقات المعلم والمدير والتصدير والتخزين تعمل داخل المتصفح. توجد حزمة Express وملف `server/index.ts` كغلاف محلي لتقديم `dist/public` ومسارات SPA، إلا أن التطبيق لا يستدعيه بوصفه API، ولا توجد طلبات شبكة تشغيلية أو استعلامات قاعدة بيانات في مسار الواجهة الحالي.

يوجد ملف OAuth متبقٍ في `client/src/const.ts` يقرأ `VITE_OAUTH_PORTAL_URL` و`VITE_APP_ID`، لكن البحث لم يظهر أي استدعاء للدالة داخـل التطبيق الحالي. لذا يعد هذا **كودًا غير مستخدم** لا ينبغي تفعيله أو تزويده بمتغيرات بيئة في Cloudflare Pages ما لم تُعاد ميزة تسجيل الدخول مستقبلًا.

| فئة الاعتماد | الحالة الحالية | أثرها على Pages |
|---|---|---|
| Backend/API | لا يوجد اعتماد فعّال | لا حاجة إلى Pages Functions أو Worker. |
| قاعدة بيانات | لا توجد قاعدة بيانات فعّالة في مسار المنتج | لا تضف `DATABASE_URL` إلى المشروع الجديد. |
| Auth/OAuth | بقايا غير مستخدمة | لا تضف متغيرات OAuth أو مفاتيحه. |
| تحليلات | لا توجد تهيئة فعّالة في HTML أو مدخل العميل | غير مطلوبة. |
| التصدير | `jsPDF` و`html2canvas` و`docx` و`xlsx` داخل العميل | يعمل على Pages دون خادم. |

## PWA والتخزين المحلي

يُنشئ `vite-plugin-pwa` manifest لتطبيق «خبير الشواهد»، ويسجل Service Worker مع Workbox لتنظيف التخزين المؤقت القديم، الاستحواذ على العملاء، والعودة إلى `/index.html` ضمن التخزين المؤقت. يلزم HTTPS لاختبار التثبيت وService Worker في بيئة الإنتاج؛ تستخدم Cloudflare Pages HTTPS ضمن إعدادات النطاق المعتادة. يجب اختبار التثبيت، وضع عدم الاتصال، والعودة المباشرة لمسار `/director` بعد أول معاينة منشورة.

تخزن رحلة المعلم المسودة والصور والشعارات في IndexedDB باسم `khabir-alshawahid-local`، مع localStorage احتياطيًا للبيانات الصغيرة ونسخ احتياطي مشفر باستخدام Web Crypto. يخزن تطبيق المدير ملفات PDF والتقييمات في IndexedDB منفصل باسم `khabir-director-local`. IndexedDB يخضع لسياسة الأصل نفسه؛ لا يستطيع النطاق الجديد قراءة بيانات النطاق القديم. [1]

| قبل نقل النطاق | الإجراء المطلوب |
|---|---|
| المعلم | تصدير النسخة الاحتياطية المشفرة من داخل التطبيق، والاحتفاظ بنسخة خارج المتصفح. |
| المدير | تنزيل ملفات PDF الأصلية وتقارير PDF/Excel التي يحتاجها؛ ملفات الوارد المحلية لا تنتقل تلقائيًا. |
| التحقق | اختبر الاستيراد/الاستعادة على النطاق الجديد بحساب تجريبي قبل دعوة المستخدمين. |

## متغيرات البيئة والأسرار

**لا توجد متغيرات بيئة مطلوبة لتشغيل النسخة الثابتة الحالية على Cloudflare Pages.** لا تضع قيمًا في إعدادات Pages إلا عند إضافة خدمة مستقبلية معتمدة فعليًا.

الملف `.project-config.json` خاص بالمنصة الحالية ويحتوي على بيانات اتصال ومفاتيح وبيانات مالك ورمز وصول إلى remote خاص. هو مستبعد من Git حاليًا، ويجب أن يبقى **خارج GitHub وخارج Cloudflare Pages**. لا تنسخ أي قيمة منه إلى ملف إعدادات جديد.

| الفئة | الأسماء التي يجب استبعادها | الإجراء |
|---|---|---|
| قاعدة البيانات | `DATABASE_URL` | لا تنقلها ولا تضفها إلى Pages. |
| مفاتيح المنصة/Forge | `BUILT_IN_FORGE_API_KEY`، `BUILT_IN_FORGE_API_URL`، `VITE_FRONTEND_FORGE_API_KEY`، `VITE_FRONTEND_FORGE_API_URL` | استبعاد كامل. |
| الجلسات وOAuth | `JWT_SECRET`، `OAUTH_SERVER_URL`، `VITE_OAUTH_PORTAL_URL`، `VITE_APP_ID` | غير مطلوبة حاليًا؛ استبعاد كامل. |
| المالك والتحليلات | `OWNER_NAME`، `OWNER_OPEN_ID`، `VITE_ANALYTICS_ENDPOINT`، `VITE_ANALYTICS_WEBSITE_ID` | استبعاد كامل. |
| تعريفات المنصة | `VITE_APP_LOGO`، `VITE_APP_TITLE`، remote/token داخل `.project-config.json` | لا تنقل القيم أو الرابط أو الرمز. |

ملف `.gitignore` الحالي يستبعد `dist/` و`node_modules/` والملفات البيئية الشائعة و`.project-config.json` وملف إصدار المنصة. قبل إنشاء مستودع GitHub نظيف، راجع تاريخ Git للتأكد من عدم وجود ملف إعداد منصة أو أسرار قديمة في سجل الالتزامات، ثم أنشئ remote جديدًا مستقلًا بدل إعادة استخدام أي remote مضمّن في إعدادات المنصة.

## خطة النقل المقترحة — بدون تنفيذ

| المرحلة | الإجراء المقترح عند الموافقة | معيار النجاح |
|---|---|---|
| 1. نسخة مصدر نظيفة | إنشاء مستودع GitHub جديد من شجرة المصدر، مع استبعاد `.project-config.json` و`dist/` و`node_modules/` وأي ملفات بيئية أو منصة. | لا تظهر أسرار أو artifacts في GitHub. |
| 2. الربط | من Cloudflare: Workers & Pages → Create application → Pages → Connect to Git، ثم اختيار المستودع والفرع `main`. يدعم Pages بناءات تلقائية عند كل push. [3] | ظهور معاينة للالتزام الأول. |
| 3. البناء | اختيار `pnpm build` ومجلد `dist/public`. لا تستخدم `npm run build` ما لم يُستبدل مدير الحزم وقفل الإصدارات. | ينتهي البناء بنجاح وتظهر أصول Vite وPWA. |
| 4. المسارات | التحقق يدويًا من `/` و`/director` عبر فتح رابط مباشر وتحديث الصفحة. يحتفظ Workbox بسلوك fallback داخل PWA بعد تثبيته؛ وأي معالجة مسارات على مستوى الاستضافة يجب اختبارها قبل الإنتاج. | لا تظهر صفحة 404 لمسار المدير. |
| 5. النطاق والـPWA | ربط نطاق إنتاجي، تفعيل HTTPS، ثم اختبار تثبيت PWA، العمل دون اتصال، واستعادة نسخة احتياطية تجريبية. | تطبيق قابل للتثبيت ويستعيد البيانات التجريبية. |
| 6. الإطلاق | تنفيذ Pilot لمجموعة صغيرة، مع تعليمات واضحة للنسخ الاحتياطي قبل تغيير النطاق. | لا فقد بيانات ولا مشكلات تصدير أو مسارات. |

Cloudflare Pages يربط المستودع ويعيد البناء والنشر تلقائيًا عند push، ويسمح بتحديد أمر البناء ومجلد الخرج، بما يناسب هذا المشروع. [2] [3]

## عناصر لا يجب تغييرها الآن

لا تنفذ نشرًا أو Git push أو تعديلًا لأوامر البناء أو إضافة `_redirects` قبل مراجعة هذا التقرير. كما لا تستخدم خادم Express في Cloudflare Pages، ولا تضف Pages Functions أو Worker ما لم تظهر حاجة حقيقية إلى API أو مصادقة أو مزامنة مستقبلية. إذا أُضيفت وظائف ديناميكية لاحقًا، فستحتاج إلى تصميم منفصل للبيانات والهوية والأسرار؛ لا ينبغي تحويل التخزين المحلي إلى قاعدة بيانات ضمن هذه الخطوة.

## قائمة قرار الإطلاق

| عنصر | الحالة الآن | ما يلزم قبل الإطلاق العام |
|---|---|---|
| الجاهزية الوظيفية | مكتملة للمنتج المحلي | اختبار قبول مع معلمين ومديرين فعليين. |
| الخصوصية | قوية افتراضيًا محليًا | نشر سياسة خصوصية وتعليمات نسخ احتياطي مبسطة. |
| قابلية النقل | جيدة | نقل المصدر إلى GitHub جديد واستبعاد إعدادات المنصة. |
| الاستضافة | مناسبة لـCloudflare Pages | معاينة ناجحة واختبار `/director` وPWA على النطاق الجديد. |
| استمرارية البيانات | حساسة لتغيير الأصل | تصدير/استيراد تجريبي قبل تغيير النطاق. |
| المراقبة والدعم | غير مكتملة للإطلاق الواسع | تحديد قناة دعم، سجل مشكلات، وخطة استرجاع للمستخدمين. |

## المراجع

[1]: https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API "MDN — IndexedDB API"
[2]: https://developers.cloudflare.com/pages/configuration/build-configuration/ "Cloudflare Pages — Build configuration"
[3]: https://developers.cloudflare.com/pages/get-started/git-integration/ "Cloudflare Pages — Git integration"
[4]: https://developers.cloudflare.com/pages/configuration/redirects/ "Cloudflare Pages — Redirects"
