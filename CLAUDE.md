# دليل المطور القادم — خبير الشواهد

> **الحالة المرجعية:** بعد اكتمال Phase 2.4 من فك الارتباط التشغيلي عن Manus/Forge. هذا الدليل لا يغيّر قواعد المنتج أو بنية البيانات.

## الغرض وحدود المنتج

«خبير الشواهد» تطبيق عربي RTL محلي أولًا لتجميع شواهد الأداء المهني وتصديرها، مع مساحة مدير مستقلة لمراجعة ملفات PDF محليًا. لا توجد مزامنة أو حسابات أو قاعدة بيانات خادمية مطلوبة لتشغيل رحلة المعلم أو المدير.

لا تضف خدمة Cloud أو API أو حسابات أو ذكاء اصطناعي إلا بقرار منتج مستقل. لا تغيّر IndexedDB أو `localStorage` أو نموذج البيانات أو محركات PDF وWord وExcel/CSV أو الطباعة أو المشاركة ضمن أعمال الصيانة العادية.

## البنية العملية

| المسار | المسؤولية |
|---|---|
| `client/src/pages/Home.tsx` | رحلة المعلم: الحزمة، الشواهد، الغلاف، التصدير، الطباعة، المشاركة والإعدادات. |
| `client/src/pages/Director.tsx` | مساحة المدير: الاستيراد، التقييم، التقرير والتصدير. |
| `client/src/lib/evidenceStore.ts` | بيانات المعلم والصور والنسخ الاحتياطية المحلية. |
| `client/src/lib/directorStore.ts` | ملفات المدير وتقييماتها المحلية. |
| `client/src/lib/exportPortfolio.ts` | PDF وWord للمعلم. |
| `client/src/lib/printPortfolio.ts` | الطباعة العربية A4. |
| `client/src/lib/brand.ts` و`ministryLogo.ts` | مراجع أصول الهوية المحلية. |
| `client/public/assets/` | رمز الهوية، الشعار النصي، شعار الوزارة، وأيقونتا PWA 192 و512. |
| `vite.config.ts` | Vite وPWA وبناء تطبيقَي المعلم والمدير؛ لا يحتوي Proxy Forge بعد Phase 2.3. |
| `scripts/` | فحوص متصفح وبناء وPWA محلية. |

## أوامر التشغيل والبناء والاختبار

| الغرض | الأمر |
|---|---|
| تطوير موحّد | `pnpm dev` |
| تطوير المعلم | `pnpm dev:teacher` |
| تطوير المدير | `pnpm dev:director` |
| فحص الأنواع | `pnpm check` |
| اختبارات الوحدة | `pnpm test` |
| بناء الإنتاج الموحد | `pnpm build` |
| تشغيل الإنتاج الموحد | `pnpm start` |
| بناء المعلم | `pnpm build:teacher` |
| بناء المدير | `pnpm build:director` |
| بناء التطبيقين | `pnpm build:apps` |
| تحقق فصل الحزمتين | `pnpm run verify:apps` |
| فحص PWA دون اتصال | `pnpm run test:pwa` |

## قواعد استقلال Manus/Forge

**تعريف النجاح:** `Operational Manus/Forge dependencies = 0`. أي لا تتطلب الواجهة أو التطوير أو بناء الإنتاج مسارات `/manus-storage/` أو Proxy Forge أو `BUILT_IN_FORGE_API_URL` أو `BUILT_IN_FORGE_API_KEY`.

أصول الهوية التشغيليّة محلية تحت `/assets/`، وManifest يشير إلى `/assets/khabir-alshawahid-store-icon-192.png` و`/assets/khabir-alshawahid-store-icon-512.png`. لا تعُد إلى مسارات `/manus-storage/` عند إضافة شعار أو أيقونة.

## الملفات الإرثية التي لا تُحذف

| الملف | الحالة | القاعدة |
|---|---|---|
| `client/src/const.ts` | OAuth ميت/إرثي | لا تحذفه ضمن أعمال Phase 2؛ لا تستورده إلى مسار تشغيلي. |
| `client/src/components/ManusDialog.tsx` | ميت/إرثي | لا تحذفه ولا تعِد تفعيله دون قرار منتج. |
| `drizzle/schema.ts` | مخطط Drizzle إرثي | لا تحذفه أو توصل قاعدة بيانات به ضمن النقل. |
| `.project-config.json` | إعداد منصة إرثي | لا تعتمد عليه في بناء أو تشغيل النسخة المحمولة. |
| `docs/ASSET_SOURCES.md` | توثيق تاريخي | لا يُعد مرجعًا تشغيليًا للأصول الحالية. |

## قواعد التغيير الآمن

قبل أي تعديل وظيفي، راجع `todo.md` وأضف بندًا قابلًا للتحقق، ثم اختبر المسار المتأثر. لا تحذف ملفات أو تبعيات لمجرد أنها تبدو غير مستخدمة. عند تعديل التصدير أو التخزين أو PWA، شغّل الاختبارات ذات الصلة ثم `pnpm check` و`pnpm build`.

> **قيد معروف:** بعض سكربتات المتصفح وفصل الحزم تستخدم مسارات Linux أو Chromium محليين؛ قابلية نقل أدوات الاختبار إلى Windows ليست شرطًا لإغلاق Phase 2، لكنها متابعة مستقلة.
